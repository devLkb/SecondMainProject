# PetChain MVP 프론트엔드 기능명세서

작성일: 2026-05-15
근거 문서: `mvp_2.md` (PetChain MVP 범위 정의)

---

## 0. 문서 개요

본 문서는 PetChain MVP에서 프론트엔드(웹 화면)가 담당해야 하는 기능을 사용자 유형별로 정리한다. API 호출, 상태 표시, 입력 처리, 권한별 노출 차이를 포함한다. 백엔드 로직과 블록체인 처리는 별도 문서를 참조한다.

대상 사용자 유형:
- 병원 사용자
- 보호자
- 보험사 사용자 (`CLAIM_REVIEWER`, `CLAIM_MANAGER`)
- 플랫폼 관리자 (`SUPPORT_ADMIN` 이상)

MVP 범위에서 제외되는 화면 (구현 금지):
- NFT/SBT 관련 화면
- 보호자 포인트/리워드 화면
- 보험상품 추천/비교/판매 화면
- 보험 가입 중개 화면
- 통계/BI 대시보드 (관리자 최소 이력 조회 제외)

---

## 1. 공통 / 인증

### 1.1 로그인 / 로그아웃 (FE-COM-001)
- 병원, 보험사, 관리자 각각 로그인 화면 제공
- 계정 ID + 비밀번호 입력
- 인증 실패 시 에러 메시지 표시
- 계정 비활성화 상태일 경우 안내
- 보험사는 API 키 인증 방식 안내 페이지도 제공

### 1.2 보호자 본인 식별 (FE-COM-002)
- 보호자는 별도 계정 로그인 또는 본인 확인 절차로 식별
- 식별 후에만 동의 관련 화면 접근 가능

### 1.3 권한 기반 라우팅 (FE-COM-003)
- 로그인된 사용자 역할에 따라 접근 가능한 화면 분기
- 보험사 사용자 중 `CLAIM_REVIEWER`, `CLAIM_MANAGER`만 제출 패키지 조회 가능
- `SUPPORT_ADMIN` 이상만 운영자 예외 조회 화면 진입 가능

### 1.4 공통 에러 메시지 표시 (FE-COM-004)
- API 응답 실패 코드별 한국어 메시지 매핑 및 표시
- 표시 대상 코드:
  - `SUBMISSION_NOT_FOUND`
  - `HOSPITAL_INVALID`
  - `INSURER_INVALID`
  - `CONSENT_MISSING`
  - `CONSENT_REVOKED`
  - `CONSENT_EXPIRED`
  - `INSURER_MISMATCH`
  - `RECORD_HASH_MISMATCH`
  - `INVALID_SUBMISSION_STATUS`
  - `UNAUTHORIZED_INSURER`
  - `FORBIDDEN_ORG_SCOPE`
  - `INSUFFICIENT_POINTS`
  - `VERIFICATION_DATA_BLOCKED_BY_CONSENT`
  - `VERIFICATION_DATA_EXPIRED`
  - `VERIFICATION_DATA_NOT_FOUND`
  - `RATE_LIMITED`

### 1.5 알림 표시 (FE-COM-005)
- 동의 만료 예정(30일/7일/1일 전), 동의 철회 알림 등 비동기 이벤트 수신 및 표시
- 이상 호출 감지 알림(관리자)

---

## 2. 병원 화면

### 2.1 병원 계정 로그인 (FE-HOS-001)
- 병원 사용자 로그인 처리
- 로그인 후 병원 대시보드 진입

### 2.2 보호자 / 반려동물 정보 관리 (FE-HOS-002)
- 보호자 최소 식별 정보 조회 또는 입력 (이름, 연락처 등)
- 반려동물 최소 정보 조회 또는 입력 (이름, 종, 식별자 등)
- 보험 가입 정보 또는 보험사 식별 정보 입력/조회

### 2.3 진료기록 등록 (FE-HOS-003)
- 진료기록 원문 입력 또는 파일 업로드
- 첨부 파일 다중 업로드 지원
- 등록 시 진료기록 메타데이터 입력 (진료일자, 진료 내용, 진료 코드 등)
- MVP에서는 표준 진료 코드 자동 매핑 없이 제한된 코드 목록 또는 수동 입력 처리
- 등록 완료 시 진료기록 식별자(`recordId`) 표시
- 해시 값은 백엔드가 계산하지만, 등록 완료 후 SHA-256 해시 값을 화면에 표시(필요 시)

### 2.4 진료기록 목록 / 상세 조회 (FE-HOS-004)
- 등록된 진료기록 목록 조회 (페이징/필터)
- 진료기록 상세 페이지에서 등록 상태, 해시, 첨부파일 목록 확인

### 2.5 제출 요청 생성 (FE-HOS-005)
- 보호자 동의가 확인된 진료기록만 제출 요청 가능 (동의 없으면 버튼 비활성화)
- 제출 대상 보험사 선택 (동의에 명시된 보험사만 선택 가능)
- 제출 요청 시 `submissionId` 생성 및 화면에 표시
- 동일 진료기록에 대해 중복 제출 차단 (이미 진행 중인 제출 존재 시 안내)

### 2.6 제출 요청 상태 확인 (FE-HOS-006)
- 제출 상태 라벨 표시:
  - `DRAFT` → "임시저장"
  - `SUBMITTED` → "제출됨"
  - `VERIFICATION_PENDING` → "검증 대기"
  - `VERIFICATION_PASSED` → "검증 완료"
  - `VERIFICATION_FAILED` → "검증 실패"
  - `PENDING_PAYMENT` → "보험사 포인트 부족(대기)"
  - `BLOCKED` → "차단됨"
  - `SUPERSEDED` → "새 버전으로 대체됨"
  - `DELETED_BY_CONSENT_WITHDRAWAL` → "동의 철회로 삭제됨"
- 검증 실패 시 실패 사유 코드 + 한국어 설명 표시
- 검증 통과 시 검증 완료 시각 표시

### 2.7 제출 패키지 버전 관리 (FE-HOS-007)
- 진료기록 원문 또는 첨부 파일 변경 시 기존 패키지 `SUPERSEDED` 표시
- 새 버전 생성 안내 메시지 표시
- 버전 이력 조회 가능

### 2.8 병원 운영 크레딧 잔액 / 이력 조회 (FE-HOS-008)
- 현재 보유 크레딧 잔액 표시
- 크레딧 거래 이력 조회 (적립, 차감, 소각, 역거래)
  - 컬럼: 거래 시각, 유형(`ACCRUAL` / `BURN` / `REVERSAL` / `CREDIT_EXPIRED`), 수량, 연관 검증 요청 ID
- 크레딧 유효기간 12개월 표시
- 만료 예정 크레딧 강조 표시

### 2.9 고급 SaaS 기능 사용권 구매 (FE-HOS-009)
- 사용권 단위: 기능 1개당 30일 사용권 = 10 크레딧
- 구매 시 크레딧 차감 확인 모달
- 구매 후 사용권 활성 기간 표시
- 플랫폼 장애로 미제공 시 `CREDIT_REVERSAL`로 복원되었음을 화면에 표시
- 병원 단순 변심 취소:
  - 서비스 시작 전: 전액 복원
  - 서비스 시작 후: 남은 기간 기준 일할 복원 안내

### 2.10 자체 웹사이트 배너 홍보 요청 (FE-HOS-010)
- 배너 유형 선택:
  - 기본 배너 7일 노출 = 20 크레딧
  - 메인 배너 7일 노출 = 50 크레딧
- 배너 컨텐츠 입력 시 금지 표현 안내 (운영자 승인 필수):
  - 의료 효과 보장 표현 금지
  - 보험금 지급 보장 표현 금지
  - 비교 우위 과장 표현 금지
- 신청 후 승인 대기 상태 표시
- 승인/거절 결과 알림 수신

### 2.11 알림 수신 (FE-HOS-011)
- 보호자 동의 만료 예정 알림 (30일 전, 7일 전, 1일 전)
- 보호자 동의 철회 알림 (5분 이내 비동기 수신)
- 검증 API 성공 시 크레딧 적립 알림 (선택)
- 사용권/배너 승인/거절 알림

---

## 3. 보호자 화면

### 3.1 보호자 본인 식별 (FE-OWN-001)
- 로그인 또는 본인 확인 후 동의 관련 화면 접근
- 본인 확인 실패 시 안내 메시지

### 3.2 동의 등록 (FE-OWN-002)
- 동의 대상 화면 구성:
  - 진료기록 단위 선택 (특정 `recordId`)
  - 보험사 단위 선택 (특정 `insurerId`)
- 동의 유효기간 1년 안내 표시
- 동의 약관 표시 및 동의 체크
- 동의 완료 시 `consentId` 생성

### 3.3 동의 상태 조회 (FE-OWN-003)
- 본인의 동의 목록 조회
- 상태 라벨:
  - `ACTIVE` → "유효"
  - `REVOKED` → "철회됨"
  - `EXPIRED` → "만료됨"
- 동의 생성 시각, 만료 예정 시각, 철회 시각 표시
- 동의별 대상 진료기록 + 대상 보험사 표시

### 3.4 동의 철회 (FE-OWN-004)
- 활성(`ACTIVE`) 상태 동의에 대해 철회 버튼 제공
- 철회 시 안내:
  - 신규 제출, 패키지 신규 조회, 원문/첨부 신규 접근, 검증 API 신규 조회 모두 차단됨
  - 이미 보험사가 다운로드/조회한 자료는 회수되지 않음
  - 체인에 남은 검증 데이터/해시/감사 이력은 삭제되지 않음
  - 플랫폼 제출 패키지는 삭제됨 (논리 삭제 즉시, 물리 삭제 30일 이내)
- 철회 확인 모달
- 철회 완료 후 `REVOKED` 상태 즉시 반영

### 3.5 재동의 (FE-OWN-005)
- 철회된 동의에 대해 재동의 가능
- 재동의는 새 동의 이벤트로 기록됨을 안내
- 재동의가 발생해도 철회로 삭제된 제출 패키지는 자동 복원되지 않음을 안내
- 보험사 제출이 필요하면 병원이 새 제출 요청을 만들어야 함을 안내

### 3.6 보험사 심사 진행 상태 확인 (FE-OWN-006)
- 본인 제출 건의 보험사 심사 진행 상태 조회 (단순 진행 수준만 노출)
- 표시 가능 상태:
  - `RECEIVED_BY_INSURER` → "보험사 접수됨"
  - `UNDER_REVIEW` → "검토 중"
  - `CLOSED` → "종료"
- `APPROVED_BY_INSURER`, `REJECTED_BY_INSURER`는 보험사가 명시적으로 외부 노출 허용한 경우에만 표시

### 3.7 알림 수신 (FE-OWN-007)
- 동의 만료 예정 알림 (30일 전, 7일 전, 1일 전)
- 심사 상태 변경 알림 (보험사 노출 허용 시)

---

## 4. 보험사 화면

### 4.1 보험사 계정 / API 인증 (FE-INS-001)
- 보험사 사용자 로그인
- API 키 발급/관리 화면(개발자/연동 담당자용)

### 4.2 검증 완료 제출 패키지 목록 (FE-INS-002)
- `VERIFICATION_PASSED` 상태의 제출 패키지 목록 조회 (별도 과금 없음)
- 컬럼: `submissionId`, 제출 시각, 검증 완료 시각, 병원명, 반려동물 식별, 상태
- 필터: 기간, 병원, 패키지 접근 상태
- 패키지 접근 상태 라벨:
  - `AVAILABLE` → "조회 가능"
  - `BLOCKED_BY_CONSENT` → "동의 철회로 조회 불가"
  - `BLOCKED_BY_PERMISSION` → "권한 없음"
  - `SUPERSEDED` → "새 버전 존재"
  - `DELETED` → "삭제됨"
  - `EXPIRED` → "만료됨"

### 4.3 제출 패키지 상세 조회 (FE-INS-003)
- 호출: `GET /submissions/{submissionId}/package`
- 노출 항목:
  - `submissionId`, `verificationId`, `recordId`
  - 병원 최소 정보
  - 보호자 최소 식별 정보
  - 반려동물 최소 정보
  - 진료기록 메타데이터
  - 진료기록 해시 + 첨부 파일별 해시
  - 동의 스냅샷
  - 검증 결과
  - 제출 요청 시각, 검증 완료 시각
  - 감사 로그 ID
- 원문/첨부 본문은 응답에 직접 포함되지 않음 — 단기 서명 URL 형태로 다운로드 버튼 제공

### 4.4 원문 / 첨부 파일 다운로드 (FE-INS-004)
- 단기 서명 URL 통해 다운로드
- 기본 만료 시간 10분 안내 표시
- 1회 다운로드 후 만료 안내
- 만료/실패 시 권한 재검증 후 재발급 버튼 제공
- 권한 검증 실패 시 명확한 사유 메시지

### 4.5 검증 API 호출 / 결과 조회 (FE-INS-005)
- 검증 실행 버튼: `POST /submissions/{submissionId}/verification`
- 호출 전 포인트 잔액 확인 안내
- 호출 전 확인 모달:
  - 성공 시 포인트 1점 차감됨을 명시
  - 동일 제출 건 반복 호출도 매번 차감됨
  - 같은 `Idempotency-Key`로 5분 이내 재시도 시 기존 결과 반환, 추가 차감 없음
- 호출 시 `Idempotency-Key` 자동 생성/지정 옵션 제공
- 응답 표시:
  - `verificationId`, `submissionId`, `status` (`PASSED` / `FAILED`)
  - `canInsurerAccessRecord`
  - `verifiedChecks` 목록
  - `failureReasons` 목록 (실패 시)
  - `deidentifiedVerificationData`:
    - `treatmentCost`, `treatmentCodes`, `diagnosisCodes`, `treatmentDate`, `hospitalId`, `insurerId`
  - `consentSnapshot`
  - `pointsCharged`, `hospitalCreditAccrued`
  - `verifiedAt`, `auditLogId`
- 검증 데이터 접근 상태 라벨:
  - `AVAILABLE` → "조회 가능"
  - `BLOCKED_BY_CONSENT` → "동의 철회/만료"
  - `BLOCKED_BY_PERMISSION` → "권한 없음"
  - `BLOCKED_BY_POINTS` → "포인트 부족"
  - `RATE_LIMITED` → "호출 제한 초과"
  - `EXPIRED` → "만료됨"

### 4.6 검증 결과 재조회 (FE-INS-006)
- `GET /verifications/{verificationId}` 호출
- 본인 조직이 호출한 검증 결과만 조회 가능
- 결과 상세 + 비식별 진료 검증 데이터 표시

### 4.7 비식별 진료 검증 데이터 별도 조회 (FE-INS-007)
- `GET /verifications/{verificationId}/deidentified-data` 호출
- 결과 패널로 비식별 데이터만 표시

### 4.8 보험사 포인트 잔액 / 이력 (FE-INS-008)
- 현재 포인트 잔액 표시
- 포인트 거래 이력:
  - 컬럼: 거래 시각, 유형(`ISSUE` / `DEDUCT` / `REVERSAL`), 수량, 연관 검증 요청 ID, 사유
- 잔액 임계치 이하 시 경고 표시
- 분당 60회 기본 호출 제한 안내

### 4.9 호출 제한 / 이상 호출 안내 (FE-INS-009)
- 분당 60회 초과 시 `RATE_LIMITED` 응답을 안내 메시지로 표시
- 동일 제출 요청에 대해 10분 내 3회 초과 성공 호출 시 운영자 검토 대상으로 표시되었음을 알림

### 4.10 보험사 심사 상태 기록 (FE-INS-010)
- 상태 변경 가능 값:
  - `RECEIVED_BY_INSURER`
  - `UNDER_REVIEW`
  - `APPROVED_BY_INSURER`
  - `REJECTED_BY_INSURER`
  - `CLOSED`
- 상태 변경 시 외부 노출 허용 여부(`APPROVED_BY_INSURER`, `REJECTED_BY_INSURER` 한정) 체크박스 제공
- 상태 변경 이력 표시
- 보험금 산정 금액, 약관 판단 근거, 내부 메모 전문, 거절 상세 법적 사유, 내부 리스크 스코어는 저장/표시 불가 — UI에 입력 필드 없음

### 4.11 동의 철회 / 만료 안내 표시 (FE-INS-011)
- 활성 제출 요청에 대한 동의 만료 예정 알림 수신
- 동의 철회 이벤트 알림 (5분 이내)
- 철회/만료 후 신규 조회 시도 시 차단 사유 메시지 표시

---

## 5. 관리자 화면

### 5.1 병원 계정 관리 (FE-ADM-001)
- 병원 계정 등록, 수정, 비활성화
- 병원 상태 변경 시 감사 로그 자동 생성 안내

### 5.2 보험사 계정 관리 (FE-ADM-002)
- 보험사 계정 등록, 수정, 비활성화
- API 키 발급 / 회수 관리

### 5.3 진료기록 제출 이력 조회 (FE-ADM-003)
- 전체 제출 요청 이력 조회
- 필터: 기간, 병원, 보험사, 상태, 검증 결과
- 컬럼: `submissionId`, 제출 시각, 검증 시각, 상태, 검증 결과, 실패 사유

### 5.4 검증 결과 / 실패 사유 조회 (FE-ADM-004)
- 검증 결과 상세 조회 (`GET /verifications/{verificationId}`)
- 실패 사유 코드별 카운트 / 빈도 표시
- 검증 결과별 감사 로그 ID 연결

### 5.5 감사 로그 조회 (FE-ADM-005)
- `GET /verifications/{verificationId}/audit` 호출
- 감사 로그 필수 표시 필드:
  - `auditLogId`, `eventType`, `actorId`, `actorOrgId`, `actorRole`
  - `resourceType`, `resourceId`
  - `submissionId`, `verificationId`
  - `result`, `failureCode`
  - `pointsDelta`, `creditDelta`
  - `createdAt`
- 모든 상태 전이 / 제출 / 조회 / 검증 / 포인트 거래 / 심사 상태 변경이 로그에 남는지 확인 가능
- 감사 로그 5년 보관 정책 안내

### 5.6 포인트 관리 (FE-ADM-006)
- 보험사 포인트 발행 화면 (Platform Org → 보험사)
- 보험사 포인트 잔액 조회
- 병원 운영 크레딧 잔액 조회
- 포인트/크레딧 거래 이력 조회:
  - 발행, 차감, 적립, 소각(`CREDIT_EXPIRED`), 역거래(`POINT_REVERSAL`, `CREDIT_REVERSAL`)

### 5.7 포인트 차감 취소 처리 (FE-ADM-007)
- `POINT_REVERSAL` 역거래 등록 화면
- 허용 조건:
  - 플랫폼 장애, 잘못된 응답, 운영자 보정이 확인된 경우만
  - 차감 시점부터 7일 이내
- 사유 입력 필수 (텍스트 + 카테고리)
- 등록 시 감사 로그 자동 기록

### 5.8 크레딧 역거래 처리 (FE-ADM-008)
- `CREDIT_REVERSAL` 등록 화면 (플랫폼 장애로 사용권/배너 미제공 시)
- 사유 입력 필수
- 등록 시 감사 로그 자동 기록

### 5.9 병원 배너 홍보 승인 (FE-ADM-009)
- 신청 목록 조회
- 컨텐츠 검토 화면
- 승인 / 거절 버튼 + 사유 입력
- 금지 표현 체크리스트:
  - 의료 효과 보장
  - 보험금 지급 보장
  - 비교 우위 과장
- 승인 시 배너 노출 시작 시각/종료 시각 설정

### 5.10 운영자 예외 조회 (FE-ADM-010)
- 동의 철회/만료 후 원문/첨부 또는 삭제 보류 제출 패키지 조회
- 접근 권한: `SUPPORT_ADMIN` 이상
- 2인 승인 워크플로우:
  - 신청자가 사유 입력 후 신청
  - 2번째 운영자가 승인 후 조회 가능
- 사유 입력 필수
- 조회 시 감사 로그 자동 기록 (조회자, 승인자, 사유, 조회 자원)

### 5.11 이상 호출 모니터링 (FE-ADM-011)
- 동일 제출 요청 10분 내 3회 초과 검증 API 성공 호출 감지 목록 표시
- 호출 보험사, 제출 요청, 호출 횟수, 시각 표시
- 운영자 검토 상태 표시 및 코멘트 입력

### 5.12 법적 보존 / 분쟁 보존 플래그 관리 (FE-ADM-012)
- 제출 패키지에 보존 플래그 설정/해제
- 플래그 설정 시 물리 삭제 보류 안내
- 외부 조회 권한은 계속 차단됨을 명시

### 5.13 보험사 / 병원 별 호출 제한 설정 (FE-ADM-013)
- 분당 호출 제한 조정 (기본 60회/분)
- 변경 이력 감사 로그 기록

### 5.14 알림 발송 정책 확인 (FE-ADM-014)
- 동의 만료 알림 발송 이력 (30일/7일/1일 전)
- 보호자/병원/보험사 발송 대상 확인
- 알림 발송 실패 건 재발송

---

## 6. 공통 UX 정책

### 6.1 상태 라벨 한국어 매핑
프론트는 모든 상태값을 사용자 유형에 맞게 한국어 라벨로 표시한다.

### 6.2 보호자 / 병원에 노출되는 보험사 심사 상태
- 기본: `RECEIVED_BY_INSURER`, `UNDER_REVIEW`, `CLOSED` 수준만 노출
- 보험사가 명시적 외부 노출 허용 시에만 `APPROVED_BY_INSURER`, `REJECTED_BY_INSURER` 노출

### 6.3 단기 서명 URL 만료
- 기본 만료 10분, 1회 다운로드 후 만료
- 화면에 만료 카운트다운 또는 만료 시각 표시
- 재발급 버튼 제공

### 6.4 포인트 차감 / 적립 안내
- 검증 API 호출 전 확인 모달 필수
- 성공 시에만 포인트 차감 / 크레딧 적립 발생을 명시
- 실패(`SUBMISSION_NOT_FOUND`, `INSUFFICIENT_POINTS`, `RATE_LIMITED` 등)는 비과금임을 명시

### 6.5 금지된 표현 / 화면 검수
- 보호자에게 보험 가입 권유, NFT/SBT 안내, 포인트 현금 환급 안내는 노출 금지
- 병원 배너 홍보 신청 화면에서 금지 표현 가이드 항시 노출

### 6.6 감사 로그 일관성
- 사용자 상태 변경, 동의 변경, 호출 결과 등 주요 액션은 화면에서 감사 로그 ID를 항상 노출하여 추적 가능

### 6.7 반응형 / 접근성
- MVP는 1개 병원, 1개 보험사 기준 구현 — 다중 기관 확장 UI는 제외
- 데스크톱 우선 (병원 / 보험사 / 관리자), 보호자 화면은 모바일 친화

### 6.8 MVP 미포함 화면 (구현 금지 재확인)
- NFT 검증 완료 증명 화면
- 반려동물 SBT 화면
- 포인트 현금 환급 / 외부 가맹점 사용
- 보험상품 추천 / 비교 / 판매
- 보험 가입 중개
- 전체 통계 / BI 대시보드 (관리자 최소 이력 조회 제외)
- 자동 표준 코드 매핑

---

## 7. MVP 구현 우선순위 (프론트 관점)

1. 병원: 진료기록 등록 + 제출 요청 화면
2. 보호자: 동의 등록 / 철회 화면
3. 병원: 제출 요청 상태 확인
4. 보험사: 검증 완료 제출 패키지 목록 / 상세
5. 보험사: 검증 API 호출 화면 + 포인트 차감 안내
6. 보험사: 포인트 잔액 / 이력 화면
7. 병원: 운영 크레딧 잔액 / 이력 화면 + 사용권/배너 신청
8. 관리자: 계정 관리 / 제출 이력 / 감사 로그 / 포인트 관리
9. 관리자: 운영자 예외 조회 (2인 승인) / 이상 호출 모니터링

이 순서는 `mvp_2.md` §10 구현 우선순위 흐름을 따른다.
